.claude/
CLAUDE.md
GETTING_STARTED.md

# build / deps
node_modules/
dist/
*.tgz

# vouch scratch file (task source #2 in docs/SPEC.md)
TASK.md

# local secrets (never committed) — e.g. ANTHROPIC_API_KEY for smoke tests
.env
.env.*
