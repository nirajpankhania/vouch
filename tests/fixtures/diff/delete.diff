diff --git a/util.ts b/util.ts
deleted file mode 100644
index d0a50b3..0000000
--- a/util.ts
+++ /dev/null
@@ -1,3 +0,0 @@
-export function clamp(n: number, lo: number, hi: number): number {
-  return Math.min(hi, Math.max(lo, n));
-}
