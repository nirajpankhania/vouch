diff --git a/clean.ts b/clean.ts
new file mode 100644
index 0000000..4f85474
--- /dev/null
+++ b/clean.ts
@@ -0,0 +1,6 @@
+export function formatLabel(input: HTMLInputElement): string {
+  const hint = input.placeholder;
+  const noop = () => {};
+  if (hint.length === 0) {}
+  return hint.trim();
+}
