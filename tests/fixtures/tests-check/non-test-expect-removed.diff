diff --git a/src/auth.ts b/src/auth.ts
index 88bcf55..5c33bcb 100644
--- a/src/auth.ts
+++ b/src/auth.ts
@@ -1,8 +1,3 @@
 export function login(user: string, pass: string): boolean {
-  expectNonEmpty(user);
   return pass === 'hunter2';
 }
-
-function expectNonEmpty(s: string): void {
-  if (!s) throw new Error('empty');
-}
